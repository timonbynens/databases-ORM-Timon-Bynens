services:
  postgres:
    image: postgres:18-alpine
    env_file:
      - .env
    ports:
      - 5432:5432
    volumes:
      - db:/var/lib/postgresql

  adminer:
    image: adminer:latest
    restart: always
    ports:
      - 8080:8080

volumes:
  db: